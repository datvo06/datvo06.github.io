<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://datvo06.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://datvo06.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-03-22T14:46:37+00:00</updated><id>https://datvo06.github.io/feed.xml</id><title type="html">Dat Nguyen</title><subtitle>Thanh-Dat Nguyen personal github page, recording what happened. </subtitle><entry><title type="html">Grammars that generalize: trying to find domain-invariant bird recognition</title><link href="https://datvo06.github.io/blog/2026/neurosymbolic-dg/" rel="alternate" type="text/html" title="Grammars that generalize: trying to find domain-invariant bird recognition"/><published>2026-03-21T10:00:00+00:00</published><updated>2026-03-21T10:00:00+00:00</updated><id>https://datvo06.github.io/blog/2026/neurosymbolic-dg</id><content type="html" xml:base="https://datvo06.github.io/blog/2026/neurosymbolic-dg/"><![CDATA[<p>Here’s a question <a href="https://github.com/Duy-Nguyen-Duc">Duy</a> and I were kicking around: what if you combined a small DSL with a neural network classifier and the classifier head itself <em>happened</em> to be domain-invariant by construction? Not as a replacement for adaptation methods like CDAN — but as a much stronger starting point for them.</p> <p>Turns out it works. We replaced a linear classification layer with a probabilistic context-free grammar over spatial layout programs and got +15pp on a hard fine-grained vision benchmark. The grammar doesn’t know about domains. It just scores spatial relations between detected parts — and those relations don’t change when you go from photos to paintings.</p> <p>Code: <a href="https://github.com/datvo06/NeuroSymbolicDG">datvo06/NeuroSymbolicDG</a>. Checkpoints: <a href="https://huggingface.co/datvo06/neurosymbolic-da-results">HuggingFace</a>.</p> <h2 id="the-grammar">The grammar</h2> <p>Let’s start with the grammar itself, since that’s the whole point.</p> <p>A standard image classifier ends with a linear layer: backbone features $\to$ class logits. We replace that with a PCFG over a small layout DSL. The DSL has two sorts of terminals:</p> <ul> <li><strong>Existence</strong>: $\texttt{has}(j)$ — “primitive $j$ is present” (returns its confidence $\in [0,1]$)</li> <li><strong>Relation</strong>: $\texttt{rel}(r, i, j)$ — “primitive $i$ is in spatial relation $r$ to primitive $j$” (returns a soft score)</li> </ul> <p>The spatial relations are scored by sigmoid/Gaussian kernels over detected primitive coordinates:</p> \[\begin{aligned} \texttt{above}(i, j) &amp;= \sigma\bigl(\lambda_1 (cy_j - cy_i - m_1)\bigr) \\ \texttt{left\_of}(i, j) &amp;= \sigma\bigl(\lambda_2 (cx_j - cx_i - m_2)\bigr) \\ \texttt{near}(i, j) &amp;= \exp\bigl(-\|c_i - c_j\|^2 / 2\rho^2\bigr) \\ \texttt{aligned\_h}(i, j) &amp;= \exp\bigl(-(cy_i - cy_j)^2 / 2\tau_1^2\bigr) \\ \texttt{aligned\_v}(i, j) &amp;= \exp\bigl(-(cx_i - cx_j)^2 / 2\tau_2^2\bigr) \\ \texttt{contains}(i, j) &amp;= \sigma\bigl(\lambda_3 \min(\text{margin}_{ij})\bigr) \end{aligned}\] <p>All parameters $(\lambda, m, \tau, \rho)$ are learnable — jointly optimized with the rest of the network. So the geometry of “above” and “near” adapts to the data; we don’t hand-code thresholds.</p> <p>Now, the <strong>grammar</strong>. Given $k=8$ primitive types and 6 relation types, the universal grammar enumerates all possible spatial constraints:</p> \[\begin{aligned} \texttt{Constraint} &amp;\to \texttt{has}(p_j) &amp;&amp; \text{for } j \in \{0, \ldots, 7\} &amp;&amp; \text{(8 productions)} \\ \texttt{Constraint} &amp;\to \texttt{rel}(r, p_i, p_j) &amp;&amp; \text{for } r \in \mathcal{R},\; i \neq j &amp;&amp; \text{(6 × 56 = 336 productions)} \\ \texttt{Layout}_y &amp;\to \texttt{choice}(\texttt{score}(w_1, c_1), \ldots, \texttt{score}(w_{344}, c_{344})) &amp;&amp; &amp;&amp; \text{(marginalization)} \end{aligned}\] <p>That’s 344 productions total. Each class $y$ has its own weight vector $\mathbf{w}_y \in \mathbb{R}^{344}$, normalized by <strong>sparsemax</strong> (<a href="https://arxiv.org/abs/1602.02068">Martins &amp; Astudillo, 2016</a>). Sparsemax is the key sparsity mechanism: unlike softmax which assigns nonzero weight to everything, sparsemax produces <em>exact zeros</em>. Each class commits to a small set of active productions — typically 4-17 out of 344, with a mean of 8.</p> <p>The class score is the weighted marginal over all productions:</p> \[W_y(x) = \sum_{p=1}^{344} \underbrace{[\text{sparsemax}(\mathbf{w}_y)]_p}_{\text{grammar weight}} \cdot \underbrace{\beta_p(x)}_{\text{spatial score}}\] <p>If you squint, this is a linear classifier — but over <em>spatial relation features</em> $\beta_p(x)$ instead of raw backbone features, with sparsemax forcing each class to pick a small structural explanation.</p> <h3 id="connection-to-neuro-symbolic-scene-understanding">Connection to neuro-symbolic scene understanding</h3> <p>This grammar is semantically similar to the visual reasoning programs used in work like Neural-Symbolic VQA (<a href="https://arxiv.org/abs/1810.02338">Yi et al., 2018</a>) and the CLEVR ecosystem (<a href="https://arxiv.org/abs/1612.06890">Johnson et al., 2017</a>). In those systems, a parser produces a symbolic program (e.g., <code class="language-plaintext highlighter-rouge">filter(red) → relate(left_of) → query(shape)</code>) that executes on a scene representation to answer questions. The programs are compositional, the scene representation is object-centric, and the spatial relations are symbolic.</p> <p>Our DSL has the same flavor — <code class="language-plaintext highlighter-rouge">rel("above", p3, p1)</code> is not far from <code class="language-plaintext highlighter-rouge">relate(above, obj3, obj1)</code> in a CLEVR-style program. But there’s a key difference in <em>how the semantics are given</em>:</p> <ul> <li><strong>CLEVR-style</strong>: the semantics are <strong>learned</strong> — a neural module network or attention mechanism learns what “left_of” means from data. This is flexible but the learned semantics can overfit to the training distribution.</li> <li><strong>Ours</strong>: the semantics are <strong>given differentiably</strong> — <code class="language-plaintext highlighter-rouge">above(i, j) = σ(λ(cy_j - cy_i - m))</code> is a fixed functional form with learnable parameters. The <em>shape</em> of the relation is specified (sigmoid on coordinate difference), but the <em>threshold and sharpness</em> adapt. This gives us a strong inductive bias: “above” really does mean “higher in the image,” it can’t drift to mean something else. And because this inductive bias is domain-agnostic — coordinate differences don’t depend on rendering style — the grammar is domain-invariant by construction.</li> </ul> <p>The tradeoff is expressiveness vs. invariance. Learned relation semantics can capture more complex patterns but may not transfer across domains. Fixed-form differentiable semantics transfer perfectly but can only express what the functional form allows. For spatial relations between detected parts, the sigmoid/Gaussian forms are expressive enough — and the domain invariance is worth it.</p> <h2 id="the-full-pipeline-on-a-real-image">The full pipeline on a real image</h2> <p>Here’s the complete pipeline running on a Painted Bunting — from detected primitives through concept heatmaps to the grammar derivation:</p> <p><img src="/assets/img/neurosymbolic_dg/full_pipeline_viz.png" alt="Full pipeline visualization" style="max-width: 100%; height: auto;"/></p> <p>Left: the 8 detected primitives overlaid on the input image. Middle: per-primitive concept heatmaps showing what each primitive attends to (bright = high activation). Right: the grammar’s derivation — 12 active productions out of 344, dominated by <code class="language-plaintext highlighter-rouge">contains</code> relations capturing spatial overlap between detected parts.</p> <p>And here’s what each grammar rule looks like spatially — the top 6 active productions visualized with their bounding boxes and relation arrows:</p> <p><img src="/assets/img/neurosymbolic_dg/grammar_rules_viz.png" alt="Grammar rules with bounding boxes" style="max-width: 100%; height: auto;"/></p> <p>Each subplot shows one active production. The colored bounding boxes show the spatial extent of each primitive (estimated from heatmap variance), and the arrows indicate which pair is being scored. The grammar captures spatial relationships between different parts of the bird — which regions overlap, which are above others.</p> <h2 id="same-primitives-different-domains">Same primitives, different domains</h2> <p>The key claim is that the grammar’s spatial structure transfers across visual domains. Here’s the same species (Painted Bunting) across Photo, Art, and Cartoon renderings, with all 8 concept heatmaps:</p> <p><img src="/assets/img/neurosymbolic_dg/cross_domain_heatmaps.png" alt="Cross-domain heatmaps" style="max-width: 100%; height: auto;"/></p> <p>The primitives detect similar spatial regions across domains — the heatmap patterns are consistent even though the pixel-level appearance changes. The grammar scores spatial relations between these primitives, and since the spatial structure holds across all renderings, the grammar produces the same classification.</p> <h2 id="what-the-grammar-actually-learns">What the grammar actually learns</h2> <p>Here’s what the trained grammar looks like on real CUB-DG checkpoints. These are the <em>actual</em> sparsemax-normalized production weights extracted from our best model (PCFG+CDAN, trained on Photo+Cartoon+Paint, tested on Art).</p> <p>Each class uses only 4-17 active productions out of 344. The grammar learns a sparse structural recipe for each bird species:</p> <p><img src="/assets/img/neurosymbolic_dg/grammar_productions.png" alt="Grammar productions per class" style="max-width: 100%; height: auto;"/></p> <p>But are the grammars actually different across classes, or do they all converge to the same pattern? Here are 5 maximally diverse species — picked by greedy selection to minimize mutual cosine similarity:</p> <p><img src="/assets/img/neurosymbolic_dg/grammar_diversity.png" alt="Grammar diversity across 5 species" style="max-width: 100%; height: auto;"/></p> <p>Very different fingerprints. Painted Bunting uses <code class="language-plaintext highlighter-rouge">left_of</code> and <code class="language-plaintext highlighter-rouge">above</code> and <code class="language-plaintext highlighter-rouge">contains</code>. Groove-billed Ani is dominated by a single strong <code class="language-plaintext highlighter-rouge">above</code> production. Gray Catbird mixes <code class="language-plaintext highlighter-rouge">contains</code> with <code class="language-plaintext highlighter-rouge">left_of</code>. Brown Creeper relies heavily on <code class="language-plaintext highlighter-rouge">left_of</code>. Each species has a distinct structural recipe — different relation types, different primitive pairs, different weight distributions.</p> <p>In fact, all 200 classes have unique active production sets (0 identical pairs), with a mean pairwise cosine similarity of just 0.04 — nearly orthogonal. Here’s the Jaccard overlap matrix for 20 random classes:</p> <p><img src="/assets/img/neurosymbolic_dg/grammar_overlap.png" alt="Grammar overlap matrix" style="max-width: 100%; height: auto;"/></p> <p>Most off-diagonal cells are pale (low overlap). The grammar learns genuinely distinct structural descriptions per species, not a one-size-fits-all template.</p> <h3 id="same-grammar-every-domain">Same grammar, every domain</h3> <p>The grammar weights are per-class, not per-domain. So the structural recipe for each species is identical whether the bird is a Photo, an Art painting, a Cartoon, or a Paint rendering — domain invariance by construction.</p> <h2 id="which-relations-survive-training">Which relations survive training?</h2> <p>We defined 6 spatial relations, but the grammar doesn’t use them equally. Here’s the breakdown across all 200 classes:</p> <p><img src="/assets/img/neurosymbolic_dg/relation_usage.png" alt="Relation usage analysis" style="max-width: 100%; height: auto;"/></p> <p><code class="language-plaintext highlighter-rouge">contains</code> dominates (94% of classes), followed by <code class="language-plaintext highlighter-rouge">above</code> (84%). <code class="language-plaintext highlighter-rouge">left_of</code> drops to 39%, and <code class="language-plaintext highlighter-rouge">aligned</code>/<code class="language-plaintext highlighter-rouge">near</code>/<code class="language-plaintext highlighter-rouge">has</code> are barely used. Why?</p> <ul> <li><strong><code class="language-plaintext highlighter-rouge">contains</code></strong> dominates — but with a caveat. The relation is <em>defined</em> as $\sigma(\lambda \min(\text{bbox margins}))$, but in practice the bounding boxes estimated from heatmap spatial variance are too diffuse for true containment. The “contains” the grammar learns is closer to a soft proximity with asymmetric margin scoring — not quite <code class="language-plaintext highlighter-rouge">near</code>, not quite geometric containment. It’s the most useful relation because the sigmoid-on-margins gives the gradient a different shape than <code class="language-plaintext highlighter-rouge">near</code>’s Gaussian-on-distance, and it’s pose-invariant: two overlapping primitives stay overlapping regardless of orientation.</li> <li><strong><code class="language-plaintext highlighter-rouge">above</code></strong> holds reliably — birds have consistent vertical structure (head above breast above belly) across most poses.</li> <li><strong><code class="language-plaintext highlighter-rouge">left_of</code></strong> drops to 39%. It’s pose-dependent: a bird facing left has its beak left-of-body; facing right, it’s reversed. The training data includes random horizontal flips (RandAugment), so the grammar learns to avoid horizontal directional relations because they’re not consistent.</li> <li><strong><code class="language-plaintext highlighter-rouge">aligned</code></strong> and <strong><code class="language-plaintext highlighter-rouge">near</code></strong> are too specific or redundant. <strong><code class="language-plaintext highlighter-rouge">has</code></strong> alone (primitive exists, no spatial context) is nearly useless — all birds have all parts.</li> </ul> <p>The grammar discovers on its own which relations are domain-invariant by driving the non-invariant ones to zero via sparsemax. Nobody told it that <code class="language-plaintext highlighter-rouge">left_of</code> is unreliable or that <code class="language-plaintext highlighter-rouge">contains</code>-as-soft-overlap is more useful than <code class="language-plaintext highlighter-rouge">near</code> — it learned this from the data.</p> <h2 id="one-program-three-semantics">One program, three semantics</h2> <p>The design principle is something that’ll look familiar if you’ve done any work with abstract interpretation: define the grammar once as an effectful program, then choose your semantics at the call site.</p> <p>The grammar is implemented using <a href="https://github.com/BasisResearch/effectful">effectful</a> algebraic effects. The five DSL operations — <code class="language-plaintext highlighter-rouge">has</code>, <code class="language-plaintext highlighter-rouge">rel</code>, <code class="language-plaintext highlighter-rouge">conj</code>, <code class="language-plaintext highlighter-rouge">choice</code>, <code class="language-plaintext highlighter-rouge">score</code> — are declared as algebraic effects via <code class="language-plaintext highlighter-rouge">@Operation.define</code>. The same grammar program runs under different handlers:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">with</span> <span class="nf">handler</span><span class="p">(</span><span class="n">eval_handler</span><span class="p">):</span>     <span class="n">score</span> <span class="o">=</span> <span class="nf">grammar</span><span class="p">(</span><span class="n">class_idx</span><span class="p">)</span>  <span class="c1"># → Tensor
</span><span class="k">with</span> <span class="nf">handler</span><span class="p">(</span><span class="n">inside_handler</span><span class="p">):</span>   <span class="n">table</span> <span class="o">=</span> <span class="nf">grammar</span><span class="p">(</span><span class="n">class_idx</span><span class="p">)</span>  <span class="c1"># → InsideTable
</span><span class="k">with</span> <span class="nf">handler</span><span class="p">(</span><span class="n">symbolic_handler</span><span class="p">):</span> <span class="n">tree</span>  <span class="o">=</span> <span class="nf">grammar</span><span class="p">(</span><span class="n">class_idx</span><span class="p">)</span>  <span class="c1"># → DerivNode
</span></code></pre></div></div> <p>The eval handler interprets the DSL in the non-negative real semiring $(\mathbb{R}_{\geq 0}, +, \times, 0, 1)$ — <code class="language-plaintext highlighter-rouge">choice</code> is addition, <code class="language-plaintext highlighter-rouge">conj</code> is multiplication, <code class="language-plaintext highlighter-rouge">score</code> scales by the grammar weight. This gives you a scalar class score, fast.</p> <p>The inside handler interprets the same program in the <em>powerset semiring</em> — tracking which subsets of primitives each subprogram explains. This is the <a href="https://en.wikipedia.org/wiki/Inside%E2%80%93outside_algorithm">inside algorithm</a> adapted from strings to sets: $I(A, S) = \sum_{A \to B\;C} w \sum_{S = S_1 \uplus S_2} I(B, S_1) \cdot I(C, S_2)$. You get exact marginals over all derivations, useful for adaptation.</p> <p>The symbolic handler builds an explicit <code class="language-plaintext highlighter-rouge">DerivNode</code> tree — that’s how we extract the derivation visualizations above.</p> <p>Same program, three abstract domains. The algebraic effects pattern makes this clean: you never modify the grammar definition, just swap the handler.</p> <p>This is the same pattern from <a href="/blog/2026/bayesian-synthesis/">my earlier post on Bayesian synthesis</a> — there we had MCMC over program structures with a PCFG prior. Here, the programs are spatial layout grammars and the data is images, but the birth/death/swap moves over derivation trees are the same idea.</p> <p>(In practice, we bypass effectful for training via <code class="language-plaintext highlighter-rouge">forward_vectorized()</code> — pure tensor ops, ~26x faster. But the handler-based version is what makes the system compositional.)</p> <h2 id="the-concrete-setup">The concrete setup</h2> <p>Take CUB-DG (<a href="https://arxiv.org/abs/2209.14108">Min et al., ECCV 2022</a>): 200 bird species across 4 visual domains (Photo, Art, Cartoon, Paint). Train on 3 domains, test on the held-out one. Best published image-only method, CORAL (<a href="https://arxiv.org/abs/1607.01719">Sun &amp; Saenko, 2016</a>): 49.9% avg. Best with image+text, CITGM: 53.6%.</p> <p>We get <strong>67.0%</strong> with image only. Here’s the full pipeline:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ResNet-50
  → 1×1 conv → k=8 heatmaps
  → spatial_soft_argmax → 8 primitives {(cx, cy, bbox, conf)}
  → PCFG: 344 productions, sparsemax weights per class
  → class scores → log-softmax
</code></pre></div></div> <p>The concept bottleneck (the 1x1 conv + <a href="https://kornia.readthedocs.io/en/latest/geometry.subpix.html">spatial soft-argmax</a>) forces the network to decompose its representation into $k=8$ spatially localized primitives. Each primitive has a location, bounding box, and confidence — the “atoms” that the grammar reasons over. No hand-designed primitive vocabulary; the primitives emerge from end-to-end training with the grammar objective.</p> <h2 id="the-grammar-as-an-abstraction">The grammar as an abstraction</h2> <p>If you think about it in PL terms, the grammar weights define a <em>finite abstraction</em> of the image. You go from a continuous pixel space to a sparse vector of spatial relation scores — a very coarse summary. Domain shift is a transformation in the concrete (pixel) domain. But the grammar’s abstraction is coarse enough to be invariant to it: “beak above breast” holds in photos and in oil paintings, even though the pixels are completely different.</p> <p>This is why it works for domain generalization, and also why you can’t improve it by adding alignment losses. The abstraction is already domain-invariant. Forcing additional invariance constraints (adversarial, MMD, etc.) just fights with the grammar’s natural behavior.</p> <h2 id="the-evidence">The evidence</h2> <p><img src="/assets/img/neurosymbolic_dg/fig1_cubdg_hero.png" alt="CUB-DG results comparison" style="max-width: 100%; height: auto;"/></p> <p>Same backbone, same data, same everything — swap the PCFG head for a linear classifier:</p> <p><img src="/assets/img/neurosymbolic_dg/fig2_pcfg_vs_nopcfg.png" alt="PCFG vs NoPCFG" style="max-width: 100%; height: auto;"/></p> <table> <thead> <tr> <th>Method</th> <th>Art</th> <th>Cartoon</th> <th>Paint</th> <th>Avg</th> </tr> </thead> <tbody> <tr> <td>NoPCFG+CDAN</td> <td>52.4%</td> <td>56.2%</td> <td>45.7%</td> <td>51.4%</td> </tr> <tr> <td><strong>PCFG+CDAN</strong></td> <td><strong>68.8%</strong></td> <td><strong>71.8%</strong></td> <td><strong>60.5%</strong></td> <td><strong>67.0%</strong></td> </tr> <tr> <td>Grammar $\Delta$</td> <td>+16.4</td> <td>+15.6</td> <td>+14.8</td> <td><strong>+15.6</strong></td> </tr> </tbody> </table> <p>+15.6pp from changing the classifier head. The gap is remarkably consistent across targets.</p> <h2 id="what-doesnt-work-and-why-thats-the-point">What doesn’t work (and why that’s the point)</h2> <p>We tried several things to improve on top of the grammar. They all made it worse:</p> <p><img src="/assets/img/neurosymbolic_dg/fig10_negative_results.png" alt="Negative results" style="max-width: 100%; height: auto;"/></p> <ul> <li><strong>Adversarial alignment</strong> (-5.5pp): 3-way domain discriminator (<a href="https://arxiv.org/abs/1505.07818">Ganin et al., 2016</a>) during training. The grammar already captures the right invariances; forcing alignment disrupts this.</li> <li><strong>Deeper grammar</strong> (-4.2pp): Hierarchical sublayouts (depth-2). Overfits to source domain structure.</li> <li><strong>Production score alignment</strong> (-12.9pp): MMD (<a href="https://jmlr.org/papers/v13/gretton12a.html">Gretton et al., 2012</a>) between production activations across domains. Redundant with the grammar’s compositional structure.</li> </ul> <p>The pattern: the grammar IS the domain invariance mechanism. Every explicit alignment attempt is either redundant or harmful. The right inductive bias does more work than the right loss function.</p> <h2 id="whats-next">What’s next</h2> <p>The grammar works because birds are compositional — parts with consistent spatial relationships. The natural question: what else is compositional enough? Documents, diagrams, multi-object scenes, maybe action recognition (body parts in motion). The framework is general; CUB-DG happened to be the right benchmark.</p> <p>The repo has training scripts, all ablations, and pretrained checkpoints on HuggingFace. If you have a compositional recognition task with domain shift, give it a try.</p>]]></content><author><name></name></author><summary type="html"><![CDATA[Combining a small DSL with a neural network for image classification gives domain invariance for free.]]></summary></entry><entry><title type="html">Bayesian synthesis of probabilistic programs for automatic data modeling</title><link href="https://datvo06.github.io/blog/2026/bayesian-synthesis/" rel="alternate" type="text/html" title="Bayesian synthesis of probabilistic programs for automatic data modeling"/><published>2026-01-23T09:00:00+00:00</published><updated>2026-01-23T09:00:00+00:00</updated><id>https://datvo06.github.io/blog/2026/bayesian-synthesis</id><content type="html" xml:base="https://datvo06.github.io/blog/2026/bayesian-synthesis/"><![CDATA[<p>Recently, I have been working on the how to combine program synthesis and probabilistic inference. Turns out that there are a variety of ways to do this:</p> <ol> <li>You can use apply probabilistic inference for program synthesis.</li> <li>You can apply program synthesis for probabilistic modeling.</li> <li>And finally, you can use probabilistic inference for program synthesis to synthesize probabilistic programs.</li> </ol> <p><a href="https://arxiv.org/abs/1907.06249">Bayesian Synthesis of Probabilistic Programs for Automatic Data Modeling</a> captures (3) quite nicely and in a self-contained manner. I tried reproducing (part of it) in a small notebook in <a href="https://github.com/datvo06/BayesianSynthesis">this repo</a>.</p> <p>While the notebook captures the implementation details of the paper, implementations aren’t everything: there are also theoretical foundations, what are some core assumptions? What are the settings in which this works well and not? I try to capture some of these in this post.</p> <h2 id="problem-statement">Problem statement</h2> <p>We start with two things:</p> <ol> <li>A language $\mathcal{L}$ (that we are going to formalize later with a Tagged <a href="https://en.wikipedia.org/wiki/Probabilistic_context-free_grammar">probabilistic context-free grammar</a>).</li> <li>A domain $\mathcal{X}$ (the space of data that we are going to model).</li> </ol> <p>The domain $\mathcal{X}$ is a set of data points $x \in \mathcal{X}$, and it also depends on which domain we are working with and is pretty straightforward to define, so we’ll focus on the tough meat: the language’s semantics.</p> <h3 id="the-languages-semantics">The Language’s semantics</h3> <p>Let’s also say that everything in this language is an expression $E \in \mathcal{L}$. The paper would define two semantics associated with this language:</p> <ol> <li><strong style="color: orange;">The prior semantic function</strong> $\texttt{Prior}: \mathcal{L} \to (0, 1]$ that calculate the prior probability of each expression.</li> <li><strong style="color: orange;">The likelihood semantic function</strong> $\texttt{Lik}: \mathcal{L} \to (\mathcal{X} \to \mathbb{R}_{\geq 0})$ that calculate the likelihood of an expression given a data point. Here, $\mathbb{R}_{\geq 0}$ is the set of non-negative real numbers.</li> </ol> <p>Intuitively, the prior would say how likely an expression is to be generated from the language, and the likelihood would say, given expression $E$, how likely it is to generate a data point $x \in \mathcal{X}$.</p> <p>$\textbf{Bounded and Normalized}$: For the prior and likelihood to be well-defined probabilistic distributions, they must be bounded (there exists a finite upper bound) and normalized (all the probabilities sum to 1). A huge chunk of the paper is devoted to saying that:</p> <blockquote> <p>Given a Tagged PCFG $\mathcal{G}$, the prior and likelihood functions are bounded and normalized.</p> </blockquote> <p><strong style="color: orange;">Posterior semantic function</strong>: Given that both the prior $\texttt{Prior}$ and the likelihood $\texttt{Lik}$ are bounded and normalized, we can define the posterior semantic function $\texttt{Post}: \mathcal{L} \to (\mathcal{X} \to \mathbb{R}_{\geq 0})$. Recall that according to Bayes’ rule: \(\mathsf{Post}\llbracket E \rrbracket(X) = \frac{\mathsf{Prior}\llbracket E \rrbracket \cdot \mathsf{Lik}\llbracket E \rrbracket(X)}{\sum_{E' \in \mathcal{L}} \mathsf{Prior}\llbracket E' \rrbracket \cdot \mathsf{Lik}\llbracket E' \rrbracket(X)}\) For each $X \in \mathcal{X}$, define $c_X ::= \sum\limits_{E \in \mathcal{L}}\mathsf{Lik}\llbracket E \rrbracket(X) \cdot \mathsf{Prior} \llbracket E \rrbracket$ to be the marginal probability of $X$ (which is finite by assumption). Then, we can rewrite the posterior semantic function as: \(\mathsf{Post}\llbracket E \rrbracket(X) = \frac{\mathsf{Prior}\llbracket E \rrbracket \cdot \mathsf{Lik}\llbracket E \rrbracket(X)}{c_X}\)</p> <p>Now we are finally ready to state the problem:</p> <p><em style="color: lightgreen;">Objective 3.5 (Bayesian Synthesis)</em> Let $\mathcal{L}$ be a language whose denotational semantics $\mathsf{Prior}$ and $\mathsf{Lik}$ satisfy 3.1., 3.2., and 3.3. Given a dataset $X \in \mathcal{X}$, generate expressions $E$ with probability $\mathsf{Post}\llbracket E \rrbracket(X)$.</p> <p>Bayesian relies on posterior distributions to infer new $E$, but here’s a problem: even though <span style="color: orange;"><em>$\mathsf{Post}\llbracket E \rrbracket(X)$</em></span> is bounded and normalized, <span style="color: orange;"><em>it is not tractable</em></span> to compute for all $E \in \mathcal{L}$ and $X \in \mathcal{X}$.</p> <h2 id="markov-chain-monte-carlo-to-the-rescue">Markov Chain Monte Carlo to the rescue</h2> <p>Bayesian inference doesn’t work if we can’t calculate the posterior distribution. But we do know how well each expression $E$ does at modeling the data point $X \in \mathcal{X}$. This is exactly the settings which Markov Chain Monte Carlo (MCMC) handles, specifically, the <a href="https://en.wikipedia.org/wiki/Metropolis%E2%80%93Hastings_algorithm">Metropolis-Hastings algorithm</a>. Only requires you to say how “relatively well” each sample is compared to the previous one, along with a proposal distribution that generate new sample from the previous one. For our case, we already know the likelihood <span style="color: orange;"><em>$\mathsf{Lik}\llbracket E \rrbracket(X)$</em></span>, we just need a proposal distribution $\mathcal{T}$ that generates new expressions $E’$ from the previous one $E$, conditioned on the language’s likelihood <span style="color: orange;"><em>$\mathsf{Lik}\llbracket E \rrbracket(X)$</em></span> and the language’s prior <span style="color: orange;"><em>$\mathsf{Prior}\llbracket E \rrbracket$</em></span>.</p> \[\begin{array}{l} \textbf{Algorithm 2: }\text{Metropolis-hasting-based sampling procedure}\\ \quad\textbf{Procedure }\text{Generate-New-Expression}(E, X)\\ \quad a \sim \text{SelectRandomElementUniformly}(A_E) \triangleright \color{orange}{\text{Randomly select a node in parse tree}}\\ \quad (N_i, E_{\text{hole}}) \leftarrow \text{Sever}_a\llbracket E \rrbracket \triangleright \color{orange}{\text{Sever the parse tree and return the non-terminal symbol at the sever point}}\\ \quad E_{\text{sub}} \sim \text{Expand}\llbracket \cdot \rrbracket(N_i) \triangleright \color{orange}{\text{Generate random } E_{\text{sub}} \text{ with probability } \text{Expand}\llbracket E_{\text{sub}} \rrbracket(N_i)}\\ \quad E' \leftarrow E_{\text{hole}}\llbracket E_{\text{sub}} \rrbracket \triangleright \color{orange}{\text{Fill hole in } E_{\text{hole}} \text{ with expression } E_{\text{sub}}}\\ \quad L \leftarrow \text{Lik}\llbracket E \rrbracket(X) \triangleright \color{orange}{\text{Evaluate likelihood for expression } E \text{ and data set } X}\\ \quad L' \leftarrow \text{Lik}\llbracket E' \rrbracket(X) \triangleright \color{orange}{\text{Evaluate likelihood for expression } E' \text{ and data set } X}\\ \quad p_{\text{accept}} \leftarrow \min\left\{1,\left(\lvert A_E\rvert/\lvert A_{E'}\rvert\right)\cdot(L'/L)\right\} \triangleright \color{orange}{\text{Compute the probability of accepting the mutation}}\\ \quad r \sim \text{UniformRandomNumber}([0, 1]) \triangleright \color{orange}{\text{Draw a random number from the unit interval}}\\ \quad \textbf{if } r &lt; p_{\text{accept}} \textbf{ then } \triangleright \color{orange}{\text{If-branch has probability } p_{\text{accept}}}\\ \quad\quad \textbf{return } E' \triangleright \color{orange}{\text{Accept and return the mutated expression}}\\ \quad \textbf{else } \triangleright \color{orange}{\text{Else-branch has probability } 1 - p_{\text{accept}}}\\ \quad\quad \textbf{return } E \triangleright \color{orange}{\text{Reject the mutated expression, and return the input expression}}\\ \end{array}\] <p>Let’s break this down:</p> <ol> <li>$\text{SelectRandomElementUniformly}(A_E)$ is a function that randomly selects a node in the parse tree $E$. When you have an expression $E$ defined in accordance with a PCFG, you can always pick a random terminal in the AST (the parse tree of the expression $E$).</li> <li>$\text{Sever}_a\llbracket E \rrbracket$ is a function that severs the parse tree $E$ at the node $a \in A_E$. Basically, one example of this is if you have an expression $E = (\cdot\ x\ y)$, and you select the node $a = (1)$ - meaning the 1st child of the root node, then $\text{Sever}_a\llbracket E \rrbracket = (Variable, (\cdot\ \square\ y)$. Where $Variable$ is a non-terminal symbol in the PCFG that are used to produce terms like $x, y, z$, etc.</li> <li>$\text{Expand}\llbracket \cdot \rrbracket(N_i)$ is a function that expands the non-terminal symbol $N_i$ in the parse tree $E$ in accordance with the PCFG’s probability distribution. We will be defining this below.</li> <li>Let’s call $\alpha(E, E’)$ the probability of accepting the mutation, defined as \(\alpha(E, E') = \min\left\{1,\left(\lvert A_E\rvert/\lvert A_{E'}\rvert\right)\cdot(L'/L)\right\}\).</li> </ol> <p>$\color{lightgreen}Question$: Why it’s called algorithm 2? Why not algorithm 1?</p> <p>$\color{lightgreen}Answer$: Algorithm 1 is the main algorithm that uses algorithm 2!</p> <p>Markov Chain in general relies on drawing many samples from the chain to approximate the stationary distribution. The more samples we draw, the more accurate the approximation. So algorithm 1:</p> \[\begin{array}{l} \textbf{Algorithm 1: }\text{Template of MCMC for Bayesian Synthesis}\\ \quad\textbf{Procedure }\text{BAYESIAN-SYNTHESIS}(X, \mathcal{T}, n)\\ \qquad\textbf{do }\text{sample }E_0 \sim \mathsf{Prior}\llbracket E_0 \rrbracket \textbf{ while }\mathsf{Lik}\llbracket E_0 \rrbracket(X) = 0\\ \quad\textbf{for }i=1 \ldots n\textbf{: } E_i \sim \mathcal{T}(X, E)\\ \quad\textbf{return }E_n \end{array}\] <p>Metropolis-Hastings relies on the property that, if the chain is irreducible (does not have any absorbing states) and aperiodic (does not have any cycles), then it will converge to a unique stationary distribution. By defining the transition based on the PCFG’s proability distribution, we can guarantee that the chain is irreducible and aperiodic.</p> <h3 id="defining-textexpandllbracket-cdot-rrbracketn_i-and-mathcalt">Defining $\text{Expand}\llbracket \cdot \rrbracket(N_i)$ and $\mathcal{T}$</h3> <p>We will now define formally the prior $\mathsf{Prior}$ and then $\mathsf{Expand}\llbracket \cdot \rrbracket(N_i)$ accordingly. For that, we need to define a tagged probabilistic context-free grammar (PCFG) $G$, in the paper:</p> <blockquote> <p>$\color{lightgreen}\textbf{Definition 4.1. (Tagged PCFG with Random Symbols)}$. Let $G = \langle \Sigma, N, R, T, P, Q, S\rangle$ denote a tagged PCFG with terminals, non-terminals, and production rules.</p> <ul> <li>$\color{lightgreen}R ::= \{R_{ik} \mid i=1, \ldots, m; k=1, \ldots, r_i\}$ is a set of production rules, where $R_{ik}$ is the $k^\text{th}$ production rule of non-terminal $N_i$. Each production rule $R_{ik}$ is a tuple of the form:</li> </ul> \[R_{ik} ::= \langle N_i, T_{ik}, \tilde{N}_1, \ldots, \tilde{N}_{h_{ik}} \rangle \qquad \color{yellow}(6)\] <p>Where $h_{ik} \geq 0$ is the numer of non-terminal on the rhs. If the set of $\tilde{N}$ is not empty, this is called <em style="color: lightgreen;">recursive production rule</em>.</p> <ul> <li>$\color{lightgreen}T$ is a set of phrase tag symbols, disjoint from $N$, where $T_{ik}$ is a unique symbol identifying the production rule $R_{ik}$</li> <li>$\color{lightgreen}P: T \to (0, 1]$: phrase tag symbols to their probabilities.</li> <li>$\color{lightgreen}Q: T \times \Sigma \to [0, 1]$ map from phrase tags and terminal symbols to probabilities, where $Q(T_{ik}, s)$ is the probaiblity that production rule $R_{ik}$ of non-terminal $N_i$ draws the terminal symbol $s \in \Sigma$. For each tag $T_{ik}$, the probabilities over symbols sum to unity $\sum\limits_{s \in \Sigma}Q(T_{ik}, s) = 1$.</li> <li>$\color{lightgreen}S \in N$ is a designated start symbol.</li> </ul> <p>We additionally assume that grammar $G$ is proper: production rules must be cycle-free, and there are no useless symbols in $\Sigma \cup N \cup T$.</p> </blockquote> <p>Basically, we would have a probability assigned to each production rule $R_{ik}$ ($Q(T_{ik}, s)$) that say, “here is the probability that we would expand in this direction”, and also a prior to say which non-terminal symbol $N_i$ we would expand to ($P(T_{ik})$).</p> <p>$\color{lightgreen}\textbf{Expand:}$ Finally, $\mathsf{Expand}: \mathcal{L}(G) \to N \to [0, 1]$ is defined inductively: \(\begin{array}{c} \mathsf{Expand}\llbracket (T_{ik}\ s) \rrbracket (N_i) := P(T_{ik}) Q(T_{ik}, s)\\ \mathsf{Expand}\llbracket (T_{ik}\ E_1 \cdots E_{h_{ik}}) \rrbracket (N_i) := P(T_{ik}) \cdot \prod_{z=1}^{h_{ik}} \mathsf{Expand} \llbracket E_z \rrbracket (\tilde N _z) \end{array}\) Where $R_{ik} = (T_{ik}, N_i, \tilde{N}_1, \ldots, \tilde{N}_{h_{ik}})$, for $i= 1,\ldots, n$ and $k= 1, \ldots, r_i$.</p> <p>$\color{lightgreen}\textbf{Proposal Distribution/Transformation Operation }\mathcal{T}:$ How do we define the operation that gives us (1) a new expression $E’$ from the current expression $E$ (so that it creates a chain) and (2) gives us the probability how likely we can obtain $E’$ from $E$?</p> <p>The authors (Feras A. Saad et al.) deals with this by severing this “transformation” (or as software engineer people often use the term “mutation” since the original infamous <a href="https://web.eecs.umich.edu/~weimerw/p/weimer-tse2012-genprog.pdf">GenProg</a> paper) into two steps: <em style="color: lightgreen;">severing</em> and <em style="color: lightgreen;">expansion</em>.</p> <p>Intuitively, <em>severing</em> means that, taken an expression $E$, you cut out part of the expression. Specifically, this is done by first creating a set of all indices for each expression $E$. In the original paper:</p> <blockquote> <p>Define the set $A ::= {(a_1, a_2, \ldots, a_l) \mid a_i \in {1, 2, \ldots, h_{\max}}, l \in {0, 1, 2, \ldots, }}$ to be a countably infinite set that index the nodes in the parse tree of $E$, where $h_{\max}$ denotes the maximum number of symbols that appear on the right of any given production rule of the in $(6)$.</p> <p>Each element $a \in A$ is a sequence of sub-expression positions on the path from the root node of a parse tree to another node. For example, if $E = (t_0\ E_1\ E_2)$ where $E_1 = (t_1\ E_3\ E_4)$ and $E_2 = (t_2\ E_5\ E_6)$, then the root node of the parse tree has index $a_\text{root} ::= ()$; the node corresponding to $E_1$ has index $(1)$, the node corresponding to $E_2$ has index $(2)$; $E_3$ and $E_4$ are $(1, 1)$ and $(1, 2)$, respectively;</p> <p>and the nodes corresponding to $E_5$, $E_6$ have $(2, 1)$ and $(2,2)$. For an expression $E$, let $A_E \subset A$ denote the finite subset of nodes that exists within the parse tree of $E$.</p> </blockquote> <p>The description of $\mathsf{Sever}$ is very interesting, and I’d recommend checking it out later. But for now, check the intuitive image below:</p> <p><img src="/assets/img/BayesianSynthesis_Sever.jpg" alt="Sever operator illustration"/></p> <p>Here, we would sever the first child of the root node, resulting in the $E_{\text{hole}} = (t_0\ \square\ E_2)$, $N_i$ would be the non-terminal symbol that produced $E_1$.</p> <p>In conjunction with $\mathsf{Sever}$, let us define $\mathsf{SubExpr}_a$ which is the sub-expression at the index $a$. $\mathsf{SubExpr}_a$ has to be well-defined, so for an index $a$ that is out of range, it should return the empty expression $\emptyset$, otherwise, is is inductively defined as follows: \(\mathsf{SubExpr}_a\llbracket(t\ E_1\ E_2\ \ldots\ E_l)\rrbracket ::= \left\{\begin{array}{ll} \emptyset&amp; \text{if \(a = ()\) or \(a_1 &gt; k\)}\\ E_j &amp; \text{if \(a = (j)\) for some $1 \leq j \leq k$} \\ \mathsf{SubExpr}_{(a_2, a_3, \ldots )} \llbracket E_j\rrbracket &amp; \text{if $i\neq (j)$ and $a_1 = j$ for some $1 \leq j \leq k$} \end{array}\right.\)</p> <p>What’s followed by severing? Finding a new $E_{sub}’$ to fill the hole. This is where the expansion comes in. From the severing process, we would have the non-terminal symbol $N_i$ that produced $E_1$. We would then expand $N_i$ in accordance with $\mathsf{Expand}\llbracket \cdot \rrbracket(N_i)$.</p> <p>Consider the probability that $\mathcal{T}$ takes an expression $E$ to another expression $E’$, which by total probability is an average over the uniformly chosen node index $a$: \(\mathcal{T}(X, E\to E') = \frac{1}{|A_E|} \sum\limits_{a \in A_E} \mathcal{T}(X, E \to E'; a) = \frac{1}{|A_E|} \sum\limits_{a \in A_E \cap A_{E'}} \mathcal{T}(X, E \to E'; a)\) where \(\mathcal{T}(X, E \to E', a) ::= \left\{ \begin{array}{l} \mathsf{Expand}\ \llbracket\mathsf{SubExpr}_a\llbracket E'\rrbracket\rrbracket(N_i) \cdot \alpha(E, E') + \mathbb{I}[E = E'](1 - \alpha(E, E'))\\ \qquad\ \text{ if \(\mathsf{Sever}_a\llbracket E\rrbracket = \mathsf{Sever}_a \llbracket E'\rrbracket = (N_i, E_\text{hole}) \) for some $i$ and $E_\text{hole}$}\\ 0\qquad\text{otherwise} \end{array} \right.\) Note that we discard terms in the sum with $a \in A_E \setminus A_{E’}$ because for these terms $\mathsf{Sever}_a\llbracket E’\rrbracket =\emptyset$ and $\mathcal{T} (X, E\to E’; a) = 0$.</p> <p>This is the end. After this, the process is pretty straightforward: we would sample a new expression $E’$ from the current expression $E$ using the Metropolis-Hastings algorithm, and then we would accept or reject the new expression based on the probability of accepting the mutation (Algorithm 2). The process is repeated for $n$ times, and we would return the last expression $E_n$.</p> <h2 id="conclusion">Conclusion</h2> <p>I tried covering the paper from a top-down perspective and I also omitted lots of details. I remember that there are moment I read the paper and just gasped at the math and how cool it is. If you find this to be interesting, I’d recommend checking out the paper for more details.</p>]]></content><author><name></name></author><summary type="html"><![CDATA[Building a chain of mutated programs for synthsizing probabilistic programs]]></summary></entry></feed>