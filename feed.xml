<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://datvo06.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://datvo06.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-07-05T21:07:09+00:00</updated><id>https://datvo06.github.io/feed.xml</id><title type="html">Dat Nguyen</title><subtitle>Dat Nguyen personal github page, recording what happened. </subtitle><entry><title type="html">Grammars that generalize: trying to find domain-invariant bird recognition</title><link href="https://datvo06.github.io/blog/2026/neurosymbolic-dg/" rel="alternate" type="text/html" title="Grammars that generalize: trying to find domain-invariant bird recognition"/><published>2026-03-21T10:00:00+00:00</published><updated>2026-03-21T10:00:00+00:00</updated><id>https://datvo06.github.io/blog/2026/neurosymbolic-dg</id><content type="html" xml:base="https://datvo06.github.io/blog/2026/neurosymbolic-dg/"><![CDATA[<p>Here’s a question <a href="https://github.com/Duy-Nguyen-Duc">Duy</a> and I were kicking around: what if you combined a small DSL with a neural network classifier and the classifier head itself <em>happened</em> to be domain-invariant by construction? Not as a replacement for adaptation methods like CDAN, but as a much stronger starting point for them.</p> <p>Turns out it works. We replaced a linear classification layer with a probabilistic context-free grammar over spatial layout programs. The grammar scores spatial relations between detected parts, and those relations don’t change when you go from photos to paintings. We call this <strong>PARSE</strong> (Primitive-Aware Relational Structure for domain gEneralization); paper at <a href="https://arxiv.org/abs/2605.06043">arXiv:2605.06043</a>.</p> <p>Code: <a href="https://github.com/datvo06/NeuroSymbolicDG">datvo06/NeuroSymbolicDG</a>. Checkpoints: <a href="https://huggingface.co/datvo06/neurosymbolic-da-results">HuggingFace</a>.</p> <h2 id="the-setup-and-the-result">The setup and the result</h2> <p>The benchmark is CUB-DG (<a href="https://arxiv.org/abs/2209.14108">Min et al., ECCV 2022</a>): 200 bird species across 4 visual domains (Photo, Art, Cartoon, Paint). Train on 3 domains, test on the held-out one. The prior best image-only method is ERM++ (<a href="https://arxiv.org/abs/2304.01973">Teterwak et al., 2025</a>) at 61.1% average; CORAL (<a href="https://arxiv.org/abs/1607.01719">Sun &amp; Saenko, 2016</a>) sits at 55.5%, GVRT (<a href="https://arxiv.org/abs/2207.02014">Min et al., 2022</a>) at 57.0%. PARSE reaches <strong>65.6%</strong> with image only.</p> <p><img src="/assets/img/neurosymbolic_dg/fig1_cubdg_hero.png" alt="CUB-DG results comparison" style="max-width: 100%; height: auto;"/></p> <p>The pipeline:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ResNet-50
  → 1×1 conv → K=16 heatmaps
  → diff. heatmap-to-coord (Nibali et al. 2018) → 16 primitives {(cx, cy, σ, δx, δy)}
        cx, cy: centroid       σ: presence confidence (sigmoid of max)
        δx, δy: spatial extent (2× std under the temperature-softmax heatmap)
  → enumerate predicate applications: ~130k for CUB-DG
  → sparsemax per-class weights, sparse dot product → class scores → softmax CE
</code></pre></div></div> <p>The concept bottleneck (1×1 conv + the differentiable heatmap-to-coordinate transform of <a href="https://arxiv.org/abs/1801.07372">Nibali et al., 2018</a>) forces the network to decompose its representation into $K=16$ spatially localized primitives. Each primitive carries a centroid, a presence confidence (sigmoid of the heatmap max), and a spatial extent (twice the coordinate-wise standard deviation under the temperature-softmax heatmap). No hand-designed primitive vocabulary: the primitives emerge from end-to-end training with image-level class labels only.</p> <p>Per the paper’s Table 1a, CUB-DG accuracy by target domain:</p> <table style="border-collapse: collapse; margin: 1em 0; font-size: 0.95em;"> <thead> <tr style="border-bottom: 2px solid #444;"> <th style="text-align: left; padding: 0.4em 0.8em;">Method</th> <th style="padding: 0.4em 0.8em;">Photo</th> <th style="padding: 0.4em 0.8em;">Cartoon</th> <th style="padding: 0.4em 0.8em;">Art</th> <th style="padding: 0.4em 0.8em;">Paint</th> <th style="padding: 0.4em 0.8em;">Avg</th> </tr> </thead> <tbody> <tr style="border-bottom: 1px solid #ddd;"> <td style="padding: 0.4em 0.8em;">CORAL (<a href="https://arxiv.org/abs/1607.01719">Sun &amp; Saenko, 2016</a>)</td> <td style="text-align: center; padding: 0.4em 0.8em;">72.2</td> <td style="text-align: center; padding: 0.4em 0.8em;">63.5</td> <td style="text-align: center; padding: 0.4em 0.8em;">50.3</td> <td style="text-align: center; padding: 0.4em 0.8em;">35.8</td> <td style="text-align: center; padding: 0.4em 0.8em;">55.5</td> </tr> <tr style="border-bottom: 1px solid #ddd;"> <td style="padding: 0.4em 0.8em;">GVRT</td> <td style="text-align: center; padding: 0.4em 0.8em;">74.6</td> <td style="text-align: center; padding: 0.4em 0.8em;">64.2</td> <td style="text-align: center; padding: 0.4em 0.8em;">52.2</td> <td style="text-align: center; padding: 0.4em 0.8em;">37.0</td> <td style="text-align: center; padding: 0.4em 0.8em;">57.0</td> </tr> <tr style="border-bottom: 1px solid #ddd;"> <td style="padding: 0.4em 0.8em;">ERM++ (prior best)</td> <td style="text-align: center; padding: 0.4em 0.8em;">60.3</td> <td style="text-align: center; padding: 0.4em 0.8em;">61.6</td> <td style="text-align: center; padding: 0.4em 0.8em;">56.8</td> <td style="text-align: center; padding: 0.4em 0.8em;"><strong>65.8</strong></td> <td style="text-align: center; padding: 0.4em 0.8em;">61.1</td> </tr> <tr style="background: #f3f5fb;"> <td style="padding: 0.4em 0.8em;"><strong>PARSE</strong></td> <td style="text-align: center; padding: 0.4em 0.8em;"><strong>75.7</strong></td> <td style="text-align: center; padding: 0.4em 0.8em;"><strong>69.1</strong></td> <td style="text-align: center; padding: 0.4em 0.8em;"><strong>68.2</strong></td> <td style="text-align: center; padding: 0.4em 0.8em;">49.3</td> <td style="text-align: center; padding: 0.4em 0.8em;"><strong>65.6</strong></td> </tr> </tbody> </table> <p>+4.5pp over ERM++ on average. The gain concentrates on the harder stylistic domains (Photo, Cartoon, Art); Paint regresses (PARSE 49.3 vs ERM++ 65.8), which I discuss later. Now let me explain what that classifier head actually is.</p> <p>Same backbone, same data, same everything. Vary the predicate arities the head can use:</p> <p><img src="/assets/img/neurosymbolic_dg/fig2_arity_ablation.png" alt="Predicate-arity ablation" style="max-width: 100%; height: auto;"/></p> <h2 id="the-grammar">The grammar</h2> <p>A standard classifier ends with a linear layer: backbone features $\to$ class logits. We replace that with a PCFG over a small layout DSL. The DSL has one unary terminal and three families of relations:</p> <ul> <li><strong>Existence</strong> (unary): <code class="language-plaintext highlighter-rouge">has</code> returns the confidence that a primitive is present.</li> <li><strong>Binary relations</strong> (6): <code class="language-plaintext highlighter-rouge">above</code>, <code class="language-plaintext highlighter-rouge">left_of</code>, <code class="language-plaintext highlighter-rouge">near</code>, <code class="language-plaintext highlighter-rouge">aligned_h</code>, <code class="language-plaintext highlighter-rouge">aligned_v</code>, <code class="language-plaintext highlighter-rouge">contains</code>. Score pairwise spatial constraints between two primitives.</li> <li><strong>Ternary relations</strong> (2): <code class="language-plaintext highlighter-rouge">tri</code> (target interior angle of a triangle of three primitives) and <code class="language-plaintext highlighter-rouge">turn</code> (target turn angle along a primitive chain).</li> <li><strong>Quaternary relations</strong> (2): <code class="language-plaintext highlighter-rouge">orient</code> (target relative orientation between two primitive-pair edges) and <code class="language-plaintext highlighter-rouge">eqdist</code> (whether the two edges have similar length, via a log-ratio).</li> </ul> <p><strong>Binary predicates.</strong> The six binary predicates are sigmoid (directional, containment) or Gaussian (proximity, alignment) over primitive centers and soft bounding boxes:</p> \[\begin{aligned} R_{\text{above}}(p_i, p_j) &amp;= \sigma\!\left(\kappa_{\uparrow} (c_j^y - c_i^y - m_{\uparrow})\right) &amp; R_{\text{left}}(p_i, p_j) &amp;= \sigma\!\left(\kappa_{\leftarrow} (c_j^x - c_i^x - m_{\leftarrow})\right) \\\\ R_{\text{h-align}}(p_i, p_j) &amp;= \exp\!\left(-\dfrac{(c_i^y - c_j^y)^2}{2\tau_h^2}\right) &amp; R_{\text{v-align}}(p_i, p_j) &amp;= \exp\!\left(-\dfrac{(c_i^x - c_j^x)^2}{2\tau_v^2}\right) \end{aligned}\] \[\begin{aligned} R_{\text{near}}(p_i, p_j) &amp;= \exp\!\left(-\dfrac{\|c_i - c_j\|^2}{2\rho^2}\right) \\\\ R_{\text{contains}}(p_i, p_j) &amp;= \sigma\!\left(\kappa_{\supset} \min\!\bigl[b_j^{x_1} - b_i^{x_1},\, b_j^{y_1} - b_i^{y_1},\, b_i^{x_2} - b_j^{x_2},\, b_i^{y_2} - b_j^{y_2}\bigr]\right) \end{aligned}\] <p>$R_\mathrm{above}$ and $R_\mathrm{left}$ score directional positions; $R_\mathrm{h\text{-}align}$ and $R_\mathrm{v\text{-}align}$ score whether two primitives share a horizontal or vertical line; $R_\mathrm{near}$ scores proximity; $R_\mathrm{contains}$ scores whether the soft bounding box of $p_i$ encloses that of $p_j$. Sharpness $\kappa$ and margins $m$, $\tau$, $\rho$ are learnable.</p> <p><strong>Ternary predicates.</strong> Each predicate is a soft Gaussian on a target angle of a primitive triple:</p> \[\begin{aligned} R_{\text{tri}}(p_i, p_j, p_k) &amp;= \exp\!\left(-\dfrac{(\alpha_{ijk} - \psi)^2}{2\beta^2}\right) &amp; R_{\text{turn}}(p_i, p_j, p_k) &amp;= \exp\!\left(-\dfrac{(\theta_{ijk} - \phi)^2}{2\eta^2}\right) \end{aligned}\] <p>where</p> \[\alpha_{ijk} = \text{interior angle at } p_i \text{ in the triangle } (p_i, p_j, p_k), \qquad \theta_{ijk} = \arccos(\hat{v}_{ij} \cdot \hat{v}_{jk}) \text{ along the chain } p_i \to p_j \to p_k.\] <p>$R_\mathrm{tri}$ scores triangular configurations against a target interior angle $\psi$; $R_\mathrm{turn}$ scores chain turns against a target turn angle $\phi$.</p> <p><strong>Quaternary predicates.</strong> Two primitive pairs are compared via their directed edges,</p> \[v_{ij} = c_j - c_i, \qquad v_{k\ell} = c_\ell - c_k.\] \[\begin{aligned} R_{\text{orient}}(p_i, p_j, p_k, p_\ell) &amp;= \exp\!\left(-\dfrac{(\hat{\mathbf{v}}_{ij} \cdot \hat{\mathbf{v}}_{k\ell} - \cos\varphi)^2}{2\gamma^2}\right) \\\\ R_{\text{eqdist}}(p_i, p_j, p_k, p_\ell) &amp;= \exp\!\left(-\dfrac{1}{2\tau_d^2} \log^2\!\dfrac{\|\mathbf{v}_{ij}\|}{\|\mathbf{v}_{k\ell}\|}\right) \end{aligned}\] <p>$R_\mathrm{orient}$ scores whether the two edges form a target relative angle $\varphi$; $R_\mathrm{eqdist}$ scores whether the two edges have similar length (the log-ratio form makes the comparison symmetric). Both are pose-and-scale invariants by construction.</p> <p>All shape parameters (sharpness $\kappa$, margins $m$, tolerances $\tau$, $\rho$, target angles $\psi$, $\phi$, $\varphi$, and tolerances $\beta$, $\eta$, $\gamma$, $\tau_d$) are learnable and jointly optimized with the rest of the network. The <em>form</em> of each predicate is locked in but the thresholds adapt.</p> <p>Given $K = 16$ primitives and the predicate vocabulary above, the grammar enumerates all valid spatial compositions (binary applied to ordered pairs, ternary to ordered triples, quaternary to ordered quadruples):</p> \[\begin{aligned} \text{Constraint} &amp;\to \text{has}(p_j) \\\\ \text{Constraint} &amp;\to \text{rel}(r, p_i, p_j) \\\\ \text{Constraint} &amp;\to \text{rel}_3(r, p_i, p_j, p_k) \\\\ \text{Constraint} &amp;\to \text{rel}_4(r, p_i, p_j, p_k, p_\ell) \\\\ \text{Layout}_y &amp;\to \text{choice}\bigl(\text{score}(w_1, c_1), \ldots, \text{score}(w_M, c_M)\bigr) \end{aligned}\] <p>The total number of enumerated productions $M$ depends on $K$ and on how many channels each higher-arity predicate spans; for CUB-DG, $M \approx 130{,}000$ (paper Table 2b). Each class $y$ has its own weight vector $\mathbf{w}_y \in \mathbb{R}^M$, normalized by <strong>sparsemax</strong> (<a href="https://arxiv.org/abs/1602.02068">Martins &amp; Astudillo, 2016</a>). Sparsemax produces <em>exact zeros</em>, so each class commits to a small set of active productions: on CUB-DG, structural compaction prunes 99.3% of weights and leaves $\sim 956$ active per class on average (paper Table 2b). The class score is</p> \[\begin{aligned} W_y(x) &amp;= \sum_{p=1}^{M} \omega_{y,p} \cdot \beta_p(x) \\\\ \text{where } \omega_{y,p} &amp;= [\mathrm{sparsemax}(\mathbf{w}_y)]_p \quad \text{(grammar weight)} \\\\ \beta_p(x) &amp;= \text{predicate activation for production } p \end{aligned}\] <p>If you squint, this is a linear classifier over <em>spatial relation features</em> with sparsemax forcing each class to pick a small structural explanation. Here’s what that looks like on a real image, a Painted Bunting, from detected primitives through heatmaps to the grammar derivation:</p> <p><img src="/assets/img/neurosymbolic_dg/full_pipeline_viz.png" alt="Full pipeline visualization" style="max-width: 100%; height: auto;"/></p> <p>And the top 6 grammar rules visualized spatially with bounding boxes:</p> <p><img src="/assets/img/neurosymbolic_dg/grammar_rules_viz.png" alt="Grammar rules with bounding boxes" style="max-width: 100%; height: auto;"/></p> <p>Every class learns a distinct grammar. Here are 5 maximally diverse species, picked by greedy selection to minimize mutual cosine similarity:</p> <p><img src="/assets/img/neurosymbolic_dg/grammar_diversity.png" alt="Grammar diversity across 5 species" style="max-width: 100%; height: auto;"/></p> <p>All 200 classes have unique active production sets (0 identical pairs), with a mean pairwise cosine similarity of just 0.04:</p> <p><img src="/assets/img/neurosymbolic_dg/grammar_overlap.png" alt="Grammar overlap matrix" style="max-width: 100%; height: auto;"/></p> <p>The grammar learns genuinely distinct structural descriptions per species, not a one-size-fits-all template. And since the weights are per-class not per-domain, the structural recipe for each species is identical across Photo, Art, Cartoon, and Paint. Domain invariance by construction.</p> <h2 id="why-it-works">Why it works</h2> <p>As a motivating example, consider the same bird species in Photo and Cartoon. The local parts (eyes, beak, wings) shift in appearance, with flattened colors, simplified textures, missing specular highlights. But their coarse spatial organization is invariant. The beak still lies near the eyes along the head; the wings still sit in characteristic positions relative to the body. A classifier that reads off “beak near eyes, wings to the side of body” gets the same signal in both domains; a classifier that reads off pixels does not.</p> <p>In PL terms, the grammar weights define a <em>finite abstraction</em> of the image. You go from a continuous pixel space to a sparse vector of spatial-predicate scores, a coarse summary. Domain shift is a transformation in the concrete (pixel) domain. The grammar’s abstraction is coarse enough to be invariant to it: “beak near eyes” and “wings beside body” hold in photos and in cartoons even though the pixels look nothing alike.</p> <p>Here’s the same species across Photo, Art, and Cartoon. The heatmap patterns are consistent despite dramatic appearance changes:</p> <p><img src="/assets/img/neurosymbolic_dg/cross_domain_heatmaps.png" alt="Cross-domain heatmaps" style="max-width: 100%; height: auto;"/></p> <p>The grammar also discovers on its own <em>which</em> relations are domain-invariant:</p> <p><img src="/assets/img/neurosymbolic_dg/relation_usage.png" alt="Relation usage analysis" style="max-width: 100%; height: auto;"/></p> <p><code class="language-plaintext highlighter-rouge">contains</code> dominates (94% of classes), <code class="language-plaintext highlighter-rouge">above</code> is second (84%), <code class="language-plaintext highlighter-rouge">left_of</code> drops to 39%. Why? <code class="language-plaintext highlighter-rouge">contains</code> (really soft overlap; the bounding boxes are too diffuse for true containment) is pose-invariant: two overlapping primitives stay overlapping regardless of orientation. <code class="language-plaintext highlighter-rouge">above</code> holds reliably across poses. But <code class="language-plaintext highlighter-rouge">left_of</code> is pose-dependent: a bird facing left has its beak left-of-body; facing right, it’s reversed. The training data includes random horizontal flips, so the grammar learns to avoid it. Nobody told it <code class="language-plaintext highlighter-rouge">left_of</code> is unreliable. Sparsemax drove it to zero.</p> <p>This is also why you can’t improve it by adding alignment losses. The abstraction is already domain-invariant. Forcing additional constraints just fights with the grammar’s natural behavior.</p> <h3 id="connection-to-neuro-symbolic-scene-understanding">Connection to neuro-symbolic scene understanding</h3> <p>The grammar is semantically close to the visual reasoning programs in Neural-Symbolic VQA (<a href="https://arxiv.org/abs/1810.02338">Yi et al., 2018</a>) and the CLEVR ecosystem (<a href="https://arxiv.org/abs/1612.06890">Johnson et al., 2017</a>). Our $\mathrm{rel}(\text{above}, p_3, p_1)$ is not far from $\mathrm{relate}(\text{above}, \mathrm{obj}_3, \mathrm{obj}_1)$ in a CLEVR-style program. One key difference in the semantics:</p> <ul> <li><strong>CLEVR-style</strong>: the semantics are <em>learned</em>. A neural module fits what “left_of” means from data. Flexible, but the relation itself can overfit to the source distribution.</li> <li><strong>Ours</strong>: the semantics are <em>given differentiably</em>. $R_\mathrm{above}(p_i, p_j) = \sigma(\kappa (c_j^y - c_i^y - m))$ is a fixed functional form with learnable parameters. The <em>shape</em> of the relation is locked in (“above” really means “higher”); only the sharpness and margin adapt. Same shape for binary, ternary, and quaternary predicates: the form is hand-specified, the thresholds train. This is what makes the structural prior domain-invariant by construction.</li> </ul> <p>The tradeoff is expressiveness for invariance. For spatial relations between detected bird parts the sigmoid / Gaussian / cosine forms are expressive enough, and the invariance is worth it.</p> <h2 id="one-program-several-semantics">One program, several semantics</h2> <p>We declare the grammar as a program over <a href="https://github.com/BasisResearch/effectful">effectful</a> algebraic ops once, then bind it to several different interpreters. The DSL surface is concrete:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># neurosymbolic_da/dsl/ops.py
</span><span class="nd">@defop</span>
<span class="k">def</span> <span class="nf">has</span><span class="p">(</span><span class="n">type_idx</span><span class="p">:</span> <span class="nb">int</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="n">Tensor</span><span class="p">:</span> <span class="bp">...</span>
<span class="nd">@defop</span>
<span class="k">def</span> <span class="nf">rel</span><span class="p">(</span><span class="n">name</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> <span class="n">a</span><span class="p">:</span> <span class="nb">int</span><span class="p">,</span> <span class="n">b</span><span class="p">:</span> <span class="nb">int</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="n">Tensor</span><span class="p">:</span> <span class="bp">...</span>
<span class="nd">@defop</span>
<span class="k">def</span> <span class="nf">conj</span><span class="p">(</span><span class="n">c1</span><span class="p">:</span> <span class="n">Tensor</span><span class="p">,</span> <span class="n">c2</span><span class="p">:</span> <span class="n">Tensor</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="n">Tensor</span><span class="p">:</span> <span class="bp">...</span>
<span class="nd">@defop</span>
<span class="k">def</span> <span class="nf">choice</span><span class="p">(</span><span class="o">*</span><span class="n">alternatives</span><span class="p">:</span> <span class="n">Tensor</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="n">Tensor</span><span class="p">:</span> <span class="bp">...</span>
<span class="nd">@defop</span>
<span class="k">def</span> <span class="nf">score</span><span class="p">(</span><span class="n">weight</span><span class="p">:</span> <span class="n">Tensor</span><span class="p">,</span> <span class="n">body</span><span class="p">:</span> <span class="n">Tensor</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="n">Tensor</span><span class="p">:</span> <span class="bp">...</span>

<span class="c1"># higher-arity ops (paper §3 ternary / quaternary)
</span><span class="nd">@defop</span>
<span class="k">def</span> <span class="nf">angle_rel</span><span class="p">(</span><span class="n">r1</span><span class="p">,</span> <span class="n">r2</span><span class="p">,</span> <span class="n">channel</span><span class="p">):</span> <span class="p">...</span>     <span class="c1"># quaternary: angle between two pair-edges
</span><span class="nd">@defop</span>
<span class="k">def</span> <span class="nf">same_distance</span><span class="p">(</span><span class="n">r1</span><span class="p">,</span> <span class="n">r2</span><span class="p">):</span> <span class="p">...</span>          <span class="c1"># quaternary: equal pair distances
</span><span class="nd">@defop</span>
<span class="k">def</span> <span class="nf">triplet_rel</span><span class="p">(</span><span class="n">r1</span><span class="p">,</span> <span class="n">r2</span><span class="p">,</span> <span class="n">r3</span><span class="p">,</span> <span class="n">channel</span><span class="p">):</span> <span class="p">...</span> <span class="c1"># ternary: triangle geometry
</span><span class="nd">@defop</span>
<span class="k">def</span> <span class="nf">chain_rel</span><span class="p">(...):</span> <span class="bp">...</span>
<span class="nd">@defop</span>
<span class="k">def</span> <span class="nf">betweenness</span><span class="p">(...):</span> <span class="bp">...</span>
<span class="nd">@defop</span>
<span class="k">def</span> <span class="nf">cross_ratio</span><span class="p">(...):</span> <span class="bp">...</span>
<span class="nd">@defop</span>
<span class="k">def</span> <span class="nf">group_rel</span><span class="p">(...):</span> <span class="bp">...</span>
</code></pre></div></div> <p>Each <code class="language-plaintext highlighter-rouge">@defop</code> declares an <em>unhandled</em> operation. The same <code class="language-plaintext highlighter-rouge">grammar(class_idx)</code> program runs under different handlers:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">with</span> <span class="nf">handler</span><span class="p">(</span><span class="nf">make_eval_handler</span><span class="p">(</span><span class="n">env</span><span class="p">,</span> <span class="n">params</span><span class="p">)):</span>
    <span class="n">score</span> <span class="o">=</span> <span class="nf">grammar</span><span class="p">(</span><span class="n">class_idx</span><span class="p">)</span>              <span class="c1"># → Tensor (forward training)
</span><span class="k">with</span> <span class="nf">handler</span><span class="p">(</span><span class="n">inside_handler</span><span class="p">):</span>
    <span class="n">table</span> <span class="o">=</span> <span class="nf">grammar</span><span class="p">(</span><span class="n">class_idx</span><span class="p">)</span>              <span class="c1"># → InsideTable (exact marginals)
</span><span class="k">with</span> <span class="nf">handler</span><span class="p">(</span><span class="n">symbolic_handler</span><span class="p">):</span>
    <span class="n">tree</span>  <span class="o">=</span> <span class="nf">grammar</span><span class="p">(</span><span class="n">class_idx</span><span class="p">)</span>              <span class="c1"># → DerivNode (visualization)
</span><span class="k">with</span> <span class="nf">handler</span><span class="p">(</span><span class="n">abstract_handler</span><span class="p">):</span>
    <span class="n">bound</span> <span class="o">=</span> <span class="nf">grammar</span><span class="p">(</span><span class="n">class_idx</span><span class="p">)</span>              <span class="c1"># → IntervalBound (robustness analysis)
</span></code></pre></div></div> <p>The handlers live in <code class="language-plaintext highlighter-rouge">dsl/handlers/</code> as <code class="language-plaintext highlighter-rouge">eval.py</code>, <code class="language-plaintext highlighter-rouge">inside.py</code>, <code class="language-plaintext highlighter-rouge">symbolic.py</code>, <code class="language-plaintext highlighter-rouge">abstract.py</code> (plus <code class="language-plaintext highlighter-rouge">cad_eval.py</code>/<code class="language-plaintext highlighter-rouge">cad_symbolic.py</code> for the CAD-style sub-language). One concrete design note worth flagging: the eval handler <em>does not</em> dispatch <code class="language-plaintext highlighter-rouge">rel</code> and <code class="language-plaintext highlighter-rouge">has</code> directly. They stay as unevaluated <code class="language-plaintext highlighter-rouge">Term</code>s and get evaluated on demand by <code class="language-plaintext highlighter-rouge">score</code> / <code class="language-plaintext highlighter-rouge">choice</code> / <code class="language-plaintext highlighter-rouge">conj</code>. This is what lets the higher-arity ops (<code class="language-plaintext highlighter-rouge">angle_rel</code>, <code class="language-plaintext highlighter-rouge">same_distance</code>, etc.) inspect the <code class="language-plaintext highlighter-rouge">Term.args</code> of their <code class="language-plaintext highlighter-rouge">rel</code> inputs and pull out the primitive indices they need, without forcing the <code class="language-plaintext highlighter-rouge">rel</code> to be evaluated to a scalar first. Without this trick, you cannot write a quaternary predicate over two binary <code class="language-plaintext highlighter-rouge">rel</code> calls without either (a) running <code class="language-plaintext highlighter-rouge">rel</code> twice and losing the index information, or (b) hand-wiring the indices outside the DSL.</p> <p>Concretely, the three main semantics are:</p> <ul> <li><strong>eval</strong>: maps each op to a non-negative real. <code class="language-plaintext highlighter-rouge">choice</code> is sum, <code class="language-plaintext highlighter-rouge">conj</code> is product, <code class="language-plaintext highlighter-rouge">score</code> multiplies by the grammar weight. Forward-pass training is just <code class="language-plaintext highlighter-rouge">evaluate(grammar(y))</code> under this handler. (Algebraically, this is the standard non-negative real interpretation: addition, multiplication, identities 0 and 1.)</li> <li><strong>inside</strong>: indexes intermediate values by <em>which subset of primitives</em> the subprogram covers, then sums weighted contributions across all assignments. Concretely, for each nonterminal A and primitive subset S,</li> </ul> \[I(A, S) = \sum_{A \to B\;C} w \sum_{S = S_1 \uplus S_2} I(B, S_1) \cdot I(C, S_2)\] <p>This is the same kind of bottom-up dynamic program as the <a href="https://en.wikipedia.org/wiki/Inside%E2%80%93outside_algorithm">inside algorithm</a> for context-free grammars on strings, with one change: the yield is a subset rather than a substring, so the split <code class="language-plaintext highlighter-rouge">S = S_1 ⊎ S_2</code> ranges over disjoint partitions of the covered set instead of contiguous substrings. The algebra is still ordinary <code class="language-plaintext highlighter-rouge">+</code> and <code class="language-plaintext highlighter-rouge">×</code>; the subset index is what differs from the eval handler. We use this to read off the exact contribution of each primitive subset to a class score, which is how the per-derivation visualizations are computed.</p> <ul> <li><strong>symbolic</strong>: doesn’t reduce the program at all; it returns an effectful <code class="language-plaintext highlighter-rouge">Term</code> tree. The <code class="language-plaintext highlighter-rouge">DerivNode</code> view rendered earlier in the post is built by walking that tree.</li> </ul> <p>The forward path used at training time is <code class="language-plaintext highlighter-rouge">forward_vectorized()</code> (<code class="language-plaintext highlighter-rouge">dsl/grammar.py:838</code>), which sidesteps the handler dispatch and computes the same eval-handler output via a fused tensor kernel. The code comment on <code class="language-plaintext highlighter-rouge">scripts/train_hybrid.py:108</code> notes it is “same as inside algorithm but ~26x faster via tensor ops.” (To be precise: at depth-1 the inside and eval outputs agree, and <code class="language-plaintext highlighter-rouge">forward_vectorized</code> matches both.) The handler-based versions are what we use for analysis and non-standard semantics; the vectorized path is what makes training tractable.</p> <p>The recurring shape (one program, multiple semantics via handlers) is the same pattern I used in <a href="/blog/2026/bayesian-synthesis/">my earlier post on Bayesian synthesis</a>. Different domain, same machinery.</p> <h2 id="what-doesnt-work-and-what-that-tells-us">What doesn’t work, and what that tells us</h2> <p>We tried several things to improve on top of the grammar. They all made it worse:</p> <p><img src="/assets/img/neurosymbolic_dg/fig10_negative_results.png" alt="Negative results" style="max-width: 100%; height: auto;"/></p> <ul> <li><strong>Adversarial alignment</strong> (-5.5pp): 3-way domain discriminator (<a href="https://arxiv.org/abs/1505.07818">Ganin et al., 2016</a>). The grammar already captures the right invariances; forcing alignment disrupts this.</li> <li><strong>Deeper grammar</strong> (-4.2pp): Hierarchical sublayouts (depth-2). Overfits to source domain structure.</li> <li><strong>Production score alignment</strong> (-12.9pp): MMD (<a href="https://jmlr.org/papers/v13/gretton12a.html">Gretton et al., 2012</a>) between production activations across domains. Redundant with the grammar’s compositional structure.</li> </ul> <p>Now let me be honest about what this all means.</p> <p><strong>“It’s just a kernel machine with hand-designed spatial features and sparsemax. Why call it a grammar?”</strong> Partly fair. At depth-1, the PCFG is a flat weighted sum over a fixed predicate-application set (~130k productions for CUB-DG, pruned to ~956 active per class after sparsemax). No recursion, no hierarchical derivation. You could rewrite the whole thing as <code class="language-plaintext highlighter-rouge">class_score = sparsemax(W) @ predicate_features(x)</code> and it would be mathematically identical. So why the grammar framing?</p> <p>Real higher-order composition (productions over productions, a binary relation feeding another binary relation) doesn’t fit a neural training loop. The parameter count blows up and gradients through stacked discrete-ish choices get noisy. Our move was to approximate that higher-order structure with explicit ternary and quaternary predicates: instead of letting the grammar recursively compose binary relations into compound relations, we enumerate a small set of higher-arity primitives that matter (triangle angles, chain turns, two-pair orientation, two-pair distance ratio) and let sparsemax pick which ones each class uses. The compositional depth lives in the predicate arity, not the derivation depth.</p> <p>The grammar framing is what produced the design either way. “Enumerate predicate applications and let sparsemax pick a sparse subset per class” is a natural move once you think in terms of productions. If we’d started from “design some spatial kernel features”, we probably wouldn’t have ended up with the handler-based architecture.</p> <p>Call it a spatial-predicate feature selector with sparsemax if you want. The results are the same.</p> <p><strong>“The depth-2 result is the most interesting finding.”</strong> Agreed. “Depth-2” in our setup means stacking a learned grouping layer over the depth-1 predicate scores. That hurt on CUB-DG because the <em>specific way</em> parts group in photos doesn’t transfer to cartoons; the grouping layer overfits to the source-domain composition. The flat version avoids this by not committing to any grouping. Real recursive composition (productions over productions) we couldn’t train usefully with backprop anyway. Where we did want higher-order structure, we baked it into the predicate vocabulary directly: ternary and quaternary predicates carry the compositional depth, so the derivation can stay flat. Whether some other domain needs a different higher-arity set is open.</p> <p><strong>“The ablation confounds architecture and features.”</strong> A fair concern, and the paper runs the clean version of it. Table 2a holds the concept bottleneck fixed and varies only the predicate vocabulary: No Rel → Binary → +Ternary → +Quaternary → All. The <em>No Rel</em> baseline uses the same primitive-detector bottleneck but feeds the descriptors straight into a linear classifier, no spatial predicates. Going from No Rel to All gives +2.7pp on average (62.9 → 65.6), with the largest gain on Photo (+3.4) and the smallest on Paint (+1.9). That is the structural-prior delta, isolated from the bottleneck delta. The headline +4.5pp over ERM++ on CUB-DG is the combined effect of both inductive biases; the +2.7pp is the part you can attribute specifically to the predicate vocabulary.</p> <p><strong>“How does this compare to concept bottleneck models?”</strong> Two differences from standard CBMs (<a href="https://arxiv.org/abs/2007.04612">Koh et al., 2020</a>). First, supervision: a CBM is trained against a predefined vocabulary of human-interpretable concepts with <em>concept-level</em> labels. PARSE is trained only with image-level class labels; the primitives emerge from end-to-end optimization, no concept supervision. Second, what sits on top: a CBM feeds an attribute vector into a linear classifier (“which concepts are present?”). PARSE feeds <em>localized</em> primitives (with spatial coordinates) into a structural scoring layer that evaluates binary, ternary, and quaternary spatial predicates (“how are the primitives spatially arranged?”). For fine-grained species recognition the arrangement is what discriminates classes, not just the presence of parts: all birds have beaks and wings; what differs is <em>where</em> they are relative to each other.</p> <p><strong>“Show me this on CLEVR or something with real compositional structure.”</strong> Fair ask. CUB-DG has compositional structure (bird parts plus spatial relations) but it isn’t CLEVR-style multi-step relational reasoning. The paper’s broader generalization story leans on DomainBed instead: PARSE hits 66.7% average across PACS / VLCS / OfficeHome / TerraIncognita / DomainNet (Table 1b), best on TerraIncognita by +3.6pp. That argues the structural prior is not just a CUB-DG artifact. We also built a synthetic compositional benchmark (SCB) in the repo (<code class="language-plaintext highlighter-rouge">neurosymbolic_da/data/scb.py</code> + PMCMC adaptation in <code class="language-plaintext highlighter-rouge">training/pmcmc.py</code>) to stress-test the compositional part directly; the grammar’s advantage there is much larger than on CUB-DG. SCB is not in the paper itself, it is an internal diagnostic. A CLEVR-DG benchmark (CLEVR scenes with rendering-style domain shift) would be the right external test for genuine compositional domain invariance, and as far as I know it does not exist yet.</p> <p><strong>“The <code class="language-plaintext highlighter-rouge">contains</code> relation doesn’t contain.”</strong> As discussed, the bounding boxes are too diffuse for true containment. What the model calls <code class="language-plaintext highlighter-rouge">contains</code> is better described as “asymmetric soft overlap.” The name is misleading, but the function is still useful.</p> <h2 id="whats-next">What’s next</h2> <p>The grammar works on CUB-DG because birds are compositional: parts with consistent spatial relationships. Same should hold for documents, diagrams, multi-object scenes, maybe action recognition. The framework is general; this is the benchmark we had.</p> <p>The depth-2 result is the part I’d most want someone else to push on. Pairwise predicates plus a small higher-arity set seem to cover what matters for birds, but I’d expect that to break on tasks with multi-scale structure (macro vs micro photography, say), where you really need group-level relations: “these parts form a unit at this scale”. I don’t know what that benchmark looks like yet.</p> <p>Repo has training scripts, ablations, and pretrained checkpoints on HuggingFace. If you have a compositional recognition task with domain shift, try it.</p>]]></content><author><name></name></author><summary type="html"><![CDATA[Combining a small DSL with a neural network for image classification gives domain invariance for free.]]></summary></entry><entry><title type="html">Bayesian synthesis of probabilistic programs for automatic data modeling</title><link href="https://datvo06.github.io/blog/2026/bayesian-synthesis/" rel="alternate" type="text/html" title="Bayesian synthesis of probabilistic programs for automatic data modeling"/><published>2026-01-23T09:00:00+00:00</published><updated>2026-01-23T09:00:00+00:00</updated><id>https://datvo06.github.io/blog/2026/bayesian-synthesis</id><content type="html" xml:base="https://datvo06.github.io/blog/2026/bayesian-synthesis/"><![CDATA[<p>Recently, I have been working on the how to combine program synthesis and probabilistic inference. Turns out that there are a variety of ways to do this:</p> <ol> <li>You can use apply probabilistic inference for program synthesis.</li> <li>You can apply program synthesis for probabilistic modeling.</li> <li>And finally, you can use probabilistic inference for program synthesis to synthesize probabilistic programs.</li> </ol> <p><a href="https://arxiv.org/abs/1907.06249">Bayesian Synthesis of Probabilistic Programs for Automatic Data Modeling</a> captures (3) quite nicely and in a self-contained manner. I tried reproducing (part of it) in a small notebook in <a href="https://github.com/datvo06/BayesianSynthesis">this repo</a>.</p> <p>While the notebook captures the implementation details of the paper, implementations aren’t everything: there are also theoretical foundations, what are some core assumptions? What are the settings in which this works well and not? I try to capture some of these in this post.</p> <h2 id="problem-statement">Problem statement</h2> <p>We start with two things:</p> <ol> <li>A language $\mathcal{L}$ (that we are going to formalize later with a Tagged <a href="https://en.wikipedia.org/wiki/Probabilistic_context-free_grammar">probabilistic context-free grammar</a>).</li> <li>A domain $\mathcal{X}$ (the space of data that we are going to model).</li> </ol> <p>The domain $\mathcal{X}$ is a set of data points $x \in \mathcal{X}$, and it also depends on which domain we are working with and is pretty straightforward to define, so we’ll focus on the tough meat: the language’s semantics.</p> <h3 id="the-languages-semantics">The Language’s semantics</h3> <p>Let’s also say that everything in this language is an expression $E \in \mathcal{L}$. The paper would define two semantics associated with this language:</p> <ol> <li><strong style="color: orange;">The prior semantic function</strong> $\texttt{Prior}: \mathcal{L} \to (0, 1]$ that calculate the prior probability of each expression.</li> <li><strong style="color: orange;">The likelihood semantic function</strong> $\texttt{Lik}: \mathcal{L} \to (\mathcal{X} \to \mathbb{R}_{\geq 0})$ that calculate the likelihood of an expression given a data point. Here, $\mathbb{R}_{\geq 0}$ is the set of non-negative real numbers.</li> </ol> <p>Intuitively, the prior would say how likely an expression is to be generated from the language, and the likelihood would say, given expression $E$, how likely it is to generate a data point $x \in \mathcal{X}$.</p> <p>$\textbf{Bounded and Normalized}$: For the prior and likelihood to be well-defined probabilistic distributions, they must be bounded (there exists a finite upper bound) and normalized (all the probabilities sum to 1). A huge chunk of the paper is devoted to saying that:</p> <blockquote> <p>Given a Tagged PCFG $\mathcal{G}$, the prior and likelihood functions are bounded and normalized.</p> </blockquote> <p><strong style="color: orange;">Posterior semantic function</strong>: Given that both the prior $\texttt{Prior}$ and the likelihood $\texttt{Lik}$ are bounded and normalized, we can define the posterior semantic function $\texttt{Post}: \mathcal{L} \to (\mathcal{X} \to \mathbb{R}_{\geq 0})$. Recall that according to Bayes’ rule: \(\mathsf{Post}\llbracket E \rrbracket(X) = \frac{\mathsf{Prior}\llbracket E \rrbracket \cdot \mathsf{Lik}\llbracket E \rrbracket(X)}{\sum_{E' \in \mathcal{L}} \mathsf{Prior}\llbracket E' \rrbracket \cdot \mathsf{Lik}\llbracket E' \rrbracket(X)}\) For each $X \in \mathcal{X}$, define $c_X ::= \sum\limits_{E \in \mathcal{L}}\mathsf{Lik}\llbracket E \rrbracket(X) \cdot \mathsf{Prior} \llbracket E \rrbracket$ to be the marginal probability of $X$ (which is finite by assumption). Then, we can rewrite the posterior semantic function as: \(\mathsf{Post}\llbracket E \rrbracket(X) = \frac{\mathsf{Prior}\llbracket E \rrbracket \cdot \mathsf{Lik}\llbracket E \rrbracket(X)}{c_X}\)</p> <p>Now we are finally ready to state the problem:</p> <p><em style="color: lightgreen;">Objective 3.5 (Bayesian Synthesis)</em> Let $\mathcal{L}$ be a language whose denotational semantics $\mathsf{Prior}$ and $\mathsf{Lik}$ satisfy 3.1., 3.2., and 3.3. Given a dataset $X \in \mathcal{X}$, generate expressions $E$ with probability $\mathsf{Post}\llbracket E \rrbracket(X)$.</p> <p>Bayesian relies on posterior distributions to infer new $E$, but here’s a problem: even though <span style="color: orange;"><em>$\mathsf{Post}\llbracket E \rrbracket(X)$</em></span> is bounded and normalized, <span style="color: orange;"><em>it is not tractable</em></span> to compute for all $E \in \mathcal{L}$ and $X \in \mathcal{X}$.</p> <h2 id="markov-chain-monte-carlo-to-the-rescue">Markov Chain Monte Carlo to the rescue</h2> <p>Bayesian inference doesn’t work if we can’t calculate the posterior distribution. But we do know how well each expression $E$ does at modeling the data point $X \in \mathcal{X}$. This is exactly the settings which Markov Chain Monte Carlo (MCMC) handles, specifically, the <a href="https://en.wikipedia.org/wiki/Metropolis%E2%80%93Hastings_algorithm">Metropolis-Hastings algorithm</a>. Only requires you to say how “relatively well” each sample is compared to the previous one, along with a proposal distribution that generate new sample from the previous one. For our case, we already know the likelihood <span style="color: orange;"><em>$\mathsf{Lik}\llbracket E \rrbracket(X)$</em></span>, we just need a proposal distribution $\mathcal{T}$ that generates new expressions $E’$ from the previous one $E$, conditioned on the language’s likelihood <span style="color: orange;"><em>$\mathsf{Lik}\llbracket E \rrbracket(X)$</em></span> and the language’s prior <span style="color: orange;"><em>$\mathsf{Prior}\llbracket E \rrbracket$</em></span>.</p> \[\begin{array}{l} \textbf{Algorithm 2: }\text{Metropolis-hasting-based sampling procedure}\\ \quad\textbf{Procedure }\text{Generate-New-Expression}(E, X)\\ \quad a \sim \text{SelectRandomElementUniformly}(A_E) \triangleright \color{orange}{\text{Randomly select a node in parse tree}}\\ \quad (N_i, E_{\text{hole}}) \leftarrow \text{Sever}_a\llbracket E \rrbracket \triangleright \color{orange}{\text{Sever the parse tree and return the non-terminal symbol at the sever point}}\\ \quad E_{\text{sub}} \sim \text{Expand}\llbracket \cdot \rrbracket(N_i) \triangleright \color{orange}{\text{Generate random } E_{\text{sub}} \text{ with probability } \text{Expand}\llbracket E_{\text{sub}} \rrbracket(N_i)}\\ \quad E' \leftarrow E_{\text{hole}}\llbracket E_{\text{sub}} \rrbracket \triangleright \color{orange}{\text{Fill hole in } E_{\text{hole}} \text{ with expression } E_{\text{sub}}}\\ \quad L \leftarrow \text{Lik}\llbracket E \rrbracket(X) \triangleright \color{orange}{\text{Evaluate likelihood for expression } E \text{ and data set } X}\\ \quad L' \leftarrow \text{Lik}\llbracket E' \rrbracket(X) \triangleright \color{orange}{\text{Evaluate likelihood for expression } E' \text{ and data set } X}\\ \quad p_{\text{accept}} \leftarrow \min\left\{1,\left(\lvert A_E\rvert/\lvert A_{E'}\rvert\right)\cdot(L'/L)\right\} \triangleright \color{orange}{\text{Compute the probability of accepting the mutation}}\\ \quad r \sim \text{UniformRandomNumber}([0, 1]) \triangleright \color{orange}{\text{Draw a random number from the unit interval}}\\ \quad \textbf{if } r &lt; p_{\text{accept}} \textbf{ then } \triangleright \color{orange}{\text{If-branch has probability } p_{\text{accept}}}\\ \quad\quad \textbf{return } E' \triangleright \color{orange}{\text{Accept and return the mutated expression}}\\ \quad \textbf{else } \triangleright \color{orange}{\text{Else-branch has probability } 1 - p_{\text{accept}}}\\ \quad\quad \textbf{return } E \triangleright \color{orange}{\text{Reject the mutated expression, and return the input expression}}\\ \end{array}\] <p>Let’s break this down:</p> <ol> <li>$\text{SelectRandomElementUniformly}(A_E)$ is a function that randomly selects a node in the parse tree $E$. When you have an expression $E$ defined in accordance with a PCFG, you can always pick a random terminal in the AST (the parse tree of the expression $E$).</li> <li>$\text{Sever}_a\llbracket E \rrbracket$ is a function that severs the parse tree $E$ at the node $a \in A_E$. Basically, one example of this is if you have an expression $E = (\cdot\ x\ y)$, and you select the node $a = (1)$ - meaning the 1st child of the root node, then $\text{Sever}_a\llbracket E \rrbracket = (Variable, (\cdot\ \square\ y)$. Where $Variable$ is a non-terminal symbol in the PCFG that are used to produce terms like $x, y, z$, etc.</li> <li>$\text{Expand}\llbracket \cdot \rrbracket(N_i)$ is a function that expands the non-terminal symbol $N_i$ in the parse tree $E$ in accordance with the PCFG’s probability distribution. We will be defining this below.</li> <li>Let’s call $\alpha(E, E’)$ the probability of accepting the mutation, defined as \(\alpha(E, E') = \min\left\{1,\left(\lvert A_E\rvert/\lvert A_{E'}\rvert\right)\cdot(L'/L)\right\}\).</li> </ol> <p>$\color{lightgreen}Question$: Why it’s called algorithm 2? Why not algorithm 1?</p> <p>$\color{lightgreen}Answer$: Algorithm 1 is the main algorithm that uses algorithm 2!</p> <p>Markov Chain in general relies on drawing many samples from the chain to approximate the stationary distribution. The more samples we draw, the more accurate the approximation. So algorithm 1:</p> \[\begin{array}{l} \textbf{Algorithm 1: }\text{Template of MCMC for Bayesian Synthesis}\\ \quad\textbf{Procedure }\text{BAYESIAN-SYNTHESIS}(X, \mathcal{T}, n)\\ \qquad\textbf{do }\text{sample }E_0 \sim \mathsf{Prior}\llbracket E_0 \rrbracket \textbf{ while }\mathsf{Lik}\llbracket E_0 \rrbracket(X) = 0\\ \quad\textbf{for }i=1 \ldots n\textbf{: } E_i \sim \mathcal{T}(X, E)\\ \quad\textbf{return }E_n \end{array}\] <p>Metropolis-Hastings relies on the property that, if the chain is irreducible (does not have any absorbing states) and aperiodic (does not have any cycles), then it will converge to a unique stationary distribution. By defining the transition based on the PCFG’s proability distribution, we can guarantee that the chain is irreducible and aperiodic.</p> <h3 id="defining-textexpandllbracket-cdot-rrbracketn_i-and-mathcalt">Defining $\text{Expand}\llbracket \cdot \rrbracket(N_i)$ and $\mathcal{T}$</h3> <p>We will now define formally the prior $\mathsf{Prior}$ and then $\mathsf{Expand}\llbracket \cdot \rrbracket(N_i)$ accordingly. For that, we need to define a tagged probabilistic context-free grammar (PCFG) $G$, in the paper:</p> <blockquote> <p>$\color{lightgreen}\textbf{Definition 4.1. (Tagged PCFG with Random Symbols)}$. Let $G = \langle \Sigma, N, R, T, P, Q, S\rangle$ denote a tagged PCFG with terminals, non-terminals, and production rules.</p> <ul> <li>$\color{lightgreen}R ::= \{R_{ik} \mid i=1, \ldots, m; k=1, \ldots, r_i\}$ is a set of production rules, where $R_{ik}$ is the $k^\text{th}$ production rule of non-terminal $N_i$. Each production rule $R_{ik}$ is a tuple of the form:</li> </ul> \[R_{ik} ::= \langle N_i, T_{ik}, \tilde{N}_1, \ldots, \tilde{N}_{h_{ik}} \rangle \qquad \color{yellow}(6)\] <p>Where $h_{ik} \geq 0$ is the numer of non-terminal on the rhs. If the set of $\tilde{N}$ is not empty, this is called <em style="color: lightgreen;">recursive production rule</em>.</p> <ul> <li>$\color{lightgreen}T$ is a set of phrase tag symbols, disjoint from $N$, where $T_{ik}$ is a unique symbol identifying the production rule $R_{ik}$</li> <li>$\color{lightgreen}P: T \to (0, 1]$: phrase tag symbols to their probabilities.</li> <li>$\color{lightgreen}Q: T \times \Sigma \to [0, 1]$ map from phrase tags and terminal symbols to probabilities, where $Q(T_{ik}, s)$ is the probaiblity that production rule $R_{ik}$ of non-terminal $N_i$ draws the terminal symbol $s \in \Sigma$. For each tag $T_{ik}$, the probabilities over symbols sum to unity $\sum\limits_{s \in \Sigma}Q(T_{ik}, s) = 1$.</li> <li>$\color{lightgreen}S \in N$ is a designated start symbol.</li> </ul> <p>We additionally assume that grammar $G$ is proper: production rules must be cycle-free, and there are no useless symbols in $\Sigma \cup N \cup T$.</p> </blockquote> <p>Basically, we would have a probability assigned to each production rule $R_{ik}$ ($Q(T_{ik}, s)$) that say, “here is the probability that we would expand in this direction”, and also a prior to say which non-terminal symbol $N_i$ we would expand to ($P(T_{ik})$).</p> <p>$\color{lightgreen}\textbf{Expand:}$ Finally, $\mathsf{Expand}: \mathcal{L}(G) \to N \to [0, 1]$ is defined inductively: \(\begin{array}{c} \mathsf{Expand}\llbracket (T_{ik}\ s) \rrbracket (N_i) := P(T_{ik}) Q(T_{ik}, s)\\ \mathsf{Expand}\llbracket (T_{ik}\ E_1 \cdots E_{h_{ik}}) \rrbracket (N_i) := P(T_{ik}) \cdot \prod_{z=1}^{h_{ik}} \mathsf{Expand} \llbracket E_z \rrbracket (\tilde N _z) \end{array}\) Where $R_{ik} = (T_{ik}, N_i, \tilde{N}_1, \ldots, \tilde{N}_{h_{ik}})$, for $i= 1,\ldots, n$ and $k= 1, \ldots, r_i$.</p> <p>$\color{lightgreen}\textbf{Proposal Distribution/Transformation Operation }\mathcal{T}:$ How do we define the operation that gives us (1) a new expression $E’$ from the current expression $E$ (so that it creates a chain) and (2) gives us the probability how likely we can obtain $E’$ from $E$?</p> <p>The authors (Feras A. Saad et al.) deals with this by severing this “transformation” (or as software engineer people often use the term “mutation” since the original infamous <a href="https://web.eecs.umich.edu/~weimerw/p/weimer-tse2012-genprog.pdf">GenProg</a> paper) into two steps: <em style="color: lightgreen;">severing</em> and <em style="color: lightgreen;">expansion</em>.</p> <p>Intuitively, <em>severing</em> means that, taken an expression $E$, you cut out part of the expression. Specifically, this is done by first creating a set of all indices for each expression $E$. In the original paper:</p> <blockquote> <p>Define the set $A ::= {(a_1, a_2, \ldots, a_l) \mid a_i \in {1, 2, \ldots, h_{\max}}, l \in {0, 1, 2, \ldots, }}$ to be a countably infinite set that index the nodes in the parse tree of $E$, where $h_{\max}$ denotes the maximum number of symbols that appear on the right of any given production rule of the in $(6)$.</p> <p>Each element $a \in A$ is a sequence of sub-expression positions on the path from the root node of a parse tree to another node. For example, if $E = (t_0\ E_1\ E_2)$ where $E_1 = (t_1\ E_3\ E_4)$ and $E_2 = (t_2\ E_5\ E_6)$, then the root node of the parse tree has index $a_\text{root} ::= ()$; the node corresponding to $E_1$ has index $(1)$, the node corresponding to $E_2$ has index $(2)$; $E_3$ and $E_4$ are $(1, 1)$ and $(1, 2)$, respectively;</p> <p>and the nodes corresponding to $E_5$, $E_6$ have $(2, 1)$ and $(2,2)$. For an expression $E$, let $A_E \subset A$ denote the finite subset of nodes that exists within the parse tree of $E$.</p> </blockquote> <p>The description of $\mathsf{Sever}$ is very interesting, and I’d recommend checking it out later. But for now, check the intuitive image below:</p> <p><img src="/assets/img/BayesianSynthesis_Sever.jpg" alt="Sever operator illustration"/></p> <p>Here, we would sever the first child of the root node, resulting in the $E_{\text{hole}} = (t_0\ \square\ E_2)$, $N_i$ would be the non-terminal symbol that produced $E_1$.</p> <p>In conjunction with $\mathsf{Sever}$, let us define $\mathsf{SubExpr}_a$ which is the sub-expression at the index $a$. $\mathsf{SubExpr}_a$ has to be well-defined, so for an index $a$ that is out of range, it should return the empty expression $\emptyset$, otherwise, is is inductively defined as follows: \(\mathsf{SubExpr}_a\llbracket(t\ E_1\ E_2\ \ldots\ E_l)\rrbracket ::= \left\{\begin{array}{ll} \emptyset&amp; \text{if \(a = ()\) or \(a_1 &gt; k\)}\\ E_j &amp; \text{if \(a = (j)\) for some $1 \leq j \leq k$} \\ \mathsf{SubExpr}_{(a_2, a_3, \ldots )} \llbracket E_j\rrbracket &amp; \text{if $i\neq (j)$ and $a_1 = j$ for some $1 \leq j \leq k$} \end{array}\right.\)</p> <p>What’s followed by severing? Finding a new $E_{sub}’$ to fill the hole. This is where the expansion comes in. From the severing process, we would have the non-terminal symbol $N_i$ that produced $E_1$. We would then expand $N_i$ in accordance with $\mathsf{Expand}\llbracket \cdot \rrbracket(N_i)$.</p> <p>Consider the probability that $\mathcal{T}$ takes an expression $E$ to another expression $E’$, which by total probability is an average over the uniformly chosen node index $a$: \(\mathcal{T}(X, E\to E') = \frac{1}{|A_E|} \sum\limits_{a \in A_E} \mathcal{T}(X, E \to E'; a) = \frac{1}{|A_E|} \sum\limits_{a \in A_E \cap A_{E'}} \mathcal{T}(X, E \to E'; a)\) where \(\mathcal{T}(X, E \to E', a) ::= \left\{ \begin{array}{l} \mathsf{Expand}\ \llbracket\mathsf{SubExpr}_a\llbracket E'\rrbracket\rrbracket(N_i) \cdot \alpha(E, E') + \mathbb{I}[E = E'](1 - \alpha(E, E'))\\ \qquad\ \text{ if \(\mathsf{Sever}_a\llbracket E\rrbracket = \mathsf{Sever}_a \llbracket E'\rrbracket = (N_i, E_\text{hole}) \) for some $i$ and $E_\text{hole}$}\\ 0\qquad\text{otherwise} \end{array} \right.\) Note that we discard terms in the sum with $a \in A_E \setminus A_{E’}$ because for these terms $\mathsf{Sever}_a\llbracket E’\rrbracket =\emptyset$ and $\mathcal{T} (X, E\to E’; a) = 0$.</p> <p>This is the end. After this, the process is pretty straightforward: we would sample a new expression $E’$ from the current expression $E$ using the Metropolis-Hastings algorithm, and then we would accept or reject the new expression based on the probability of accepting the mutation (Algorithm 2). The process is repeated for $n$ times, and we would return the last expression $E_n$.</p> <h2 id="conclusion">Conclusion</h2> <p>I tried covering the paper from a top-down perspective and I also omitted lots of details. I remember that there are moment I read the paper and just gasped at the math and how cool it is. If you find this to be interesting, I’d recommend checking out the paper for more details.</p>]]></content><author><name></name></author><summary type="html"><![CDATA[Building a chain of mutated programs for synthsizing probabilistic programs]]></summary></entry></feed>